import 'package:flutter/material.dart';

class Category {
  final id;
  final title;
  final imageUrl;

  const Category(
      {@required this.id, @required this.title, @required this.imageUrl});
}
